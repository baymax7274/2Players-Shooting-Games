# v1.2.0 动态环境系统 — 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为双人对战射击游戏添加拾取物、可破坏墙、移动墙和5张地图专属动态机制

**Architecture:** 新增3个模块（Pickup、PickupManager、Environment），扩展GameMap支持tile破坏和移动墙，在BattleScene中串联调用。所有逻辑驱动由JSON配置。

**Tech Stack:** Python 3.10+ / Pygame 2.x / pytest

---

### 前置：确保工作区隔离

- [ ] **Step 1: 创建特性分支**

```bash
cd "D:\ClaudeCode Program Environment\2Players Shooting Games"
git checkout -b feature/v1.2.0-dynamic-environment
```

---

### Task 1: config.py 新增常量

**Files:**
- Modify: `config.py`

所有新系统需要的常量，一处定义。

- [ ] **Step 1: 在 config.py 末尾追加常量**

```python
# ========== v1.2.0 动态环境 ==========

# 拾取物
PICKUP_RESPAWN_HEALTH = 20.0
PICKUP_RESPAWN_POWER = 45.0
PICKUP_RESPAWN_SPEED = 40.0
PICKUP_MAX_HEALTH = 3
PICKUP_MAX_POWER = 1
PICKUP_MAX_SPEED = 1
PICKUP_RADIUS = 12

# Power-up 持续时间
POWER_DURATION = 8.0
SPEED_DURATION = 6.0
HEAL_AMOUNT = 25

# 可破坏环境
DESTRUCTIBLE_FLASH_TIME = 0.1
SHOTGUN_DESTROY_RANGE = 100.0

# 移动墙
MOVING_WALL_SPEED_MIN = 60
MOVING_WALL_SPEED_MAX = 100
MOVING_WALL_PAUSE = 0.5

# 环境事件
COLLAPSE_INTERVAL_MIN = 25.0
COLLAPSE_INTERVAL_MAX = 35.0
POISON_START_TIME = 60.0
POISON_INITIAL_RADIUS = 2
POISON_EXPAND_INTERVAL = 15.0
POISON_EXPAND_STEP = 1
POISON_DPS = 5.0
GRAVITY_FLIP_INTERVAL = 15.0
THORN_ACTIVATE_INTERVAL = 20.0
THORN_ACTIVATE_COUNT = 3
THORN_DURATION = 5.0
THORN_DAMAGE = 15

# 移动墙推动玩家的力系数
WALL_PUSH_FORCE = 0.8
```

- [ ] **Step 2: 验证导入无语法错误**

```bash
cd "D:\ClaudeCode Program Environment\2Players Shooting Games"
python -c "import config; print('PICKUP_RESPAWN_HEALTH:', config.PICKUP_RESPAWN_HEALTH)"
```

- [ ] **Step 3: 提交**

```bash
git add config.py
git commit -m "feat(config): v1.2.0 动态环境系统常量定义"
```

---

### Task 2: Pickup 实体类

**Files:**
- Create: `src/entities/pickup.py`

单个拾取物实体，包含类型定义、浮动动画、渲染。

- [ ] **Step 1: 创建 Pickup 类**

```python
import math
import pygame
from config import PICKUP_RADIUS, COLOR_OUTLINE
from src.core.vector import Vec2


PICKUP_TYPES = {
    "health": {
        "color": (255, 50, 50),
        "respawn": 20.0,
        "max_active": 3,
    },
    "power": {
        "color": (180, 50, 220),
        "respawn": 45.0,
        "max_active": 1,
    },
    "speed": {
        "color": (50, 220, 50),
        "respawn": 40.0,
        "max_active": 1,
    },
}


class Pickup:
    def __init__(self, pickup_type, position):
        self.pickup_type = pickup_type
        self.pos = Vec2(*position)
        self.radius = PICKUP_RADIUS
        self.alive = True
        self.defn = PICKUP_TYPES[pickup_type]
        self.color = self.defn["color"]
        self.bob_phase = 0.0

    def update(self, dt):
        self.bob_phase += dt * 3.0

    def render(self, screen, camera_offset):
        sx = int(self.pos.x - camera_offset.x)
        sy = int(self.pos.y - camera_offset.y + math.sin(self.bob_phase) * 3)

        # 外圈光晕
        glow = pygame.Surface((self.radius * 4, self.radius * 4), pygame.SRCALPHA)
        alpha_pulse = int(50 + 30 * math.sin(self.bob_phase * 1.5))
        pygame.draw.circle(
            glow, (*self.color, alpha_pulse),
            (self.radius * 2, self.radius * 2), self.radius + 4
        )
        screen.blit(glow, (sx - self.radius * 2, sy - self.radius * 2))

        # 描边
        pygame.draw.circle(screen, COLOR_OUTLINE, (sx, sy), self.radius + 1)
        # 主体
        pygame.draw.circle(screen, self.color, (sx, sy), self.radius)
        # 高光
        lighter = tuple(min(c + 80, 255) for c in self.color)
        pygame.draw.circle(screen, lighter, (sx - 2, sy - 3), self.radius - 5)

        # 类型图标：血包=十字，强力=星形，加速=箭头
        if self.pickup_type == "health":
            pygame.draw.rect(screen, (255, 255, 255),
                             (sx - 2, sy - 6, 4, 12))
            pygame.draw.rect(screen, (255, 255, 255),
                             (sx - 6, sy - 2, 12, 4))
        elif self.pickup_type == "power":
            for i in range(4):
                angle = math.pi / 2 * i + self.bob_phase
                ex = sx + int(math.cos(angle) * 6)
                ey = sy + int(math.sin(angle) * 6)
                pygame.draw.line(screen, (255, 255, 100), (sx, sy), (ex, ey), 2)
        elif self.pickup_type == "speed":
            pts = [
                (sx - 5, sy + 5), (sx, sy - 6), (sx + 5, sy + 5),
                (sx - 2, sy + 1), (sx + 2, sy + 1),
            ]
            pygame.draw.polygon(screen, (255, 255, 100), pts[:3])
            pygame.draw.polygon(screen, (255, 255, 100), pts[3:])
```

- [ ] **Step 2: 验证导入**

```bash
cd "D:\ClaudeCode Program Environment\2Players Shooting Games"
python -c "from src.entities.pickup import Pickup, PICKUP_TYPES; p = Pickup('health', (100, 100)); print('OK', p.color)"
```

- [ ] **Step 3: 提交**

```bash
git add src/entities/pickup.py
git commit -m "feat: 添加 Pickup 拾取物实体类"
```

---

### Task 3: PickupManager 拾取物管理器

**Files:**
- Create: `src/systems/pickup_manager.py`

管理拾取物的刷新、碰撞检测和效果应用。

- [ ] **Step 1: 创建 PickupManager**

```python
import random
from config import (
    PICKUP_RESPAWN_HEALTH, PICKUP_RESPAWN_POWER, PICKUP_RESPAWN_SPEED,
    PICKUP_MAX_HEALTH, PICKUP_MAX_POWER, PICKUP_MAX_SPEED,
    POWER_DURATION, SPEED_DURATION, HEAL_AMOUNT,
)
from src.entities.pickup import Pickup, PICKUP_TYPES
from src.core.physics import circle_circle_collision


class PickupManager:
    def __init__(self):
        self.pickups = []
        self.spawn_timers = {}
        self.spawn_points = []
        self.map_name = ""

    def setup(self, map_name, spawn_points):
        self.map_name = map_name
        self.spawn_points = list(spawn_points)
        self.pickups.clear()
        self.spawn_timers = {
            "health": 0.0,
            "power": 0.0,
            "speed": 0.0,
        }

    def _count_active(self, pickup_type):
        return sum(1 for p in self.pickups
                   if p.pickup_type == pickup_type and p.alive)

    def _get_random_spawn(self):
        if not self.spawn_points:
            return None
        pt = random.choice(self.spawn_points)
        return (pt["x"] * 32 + 16, pt["y"] * 32 + 16)

    def update(self, dt, players, game_map):
        # 更新计时器并尝试刷新
        for ptype, defn in PICKUP_TYPES.items():
            self.spawn_timers[ptype] -= dt
            max_active = defn["max_active"]
            if self.spawn_timers[ptype] <= 0:
                if self._count_active(ptype) < max_active:
                    pos = self._get_random_spawn()
                    if pos is not None:
                        # 确保刷新点在空地上
                        tx = int(pos[0] // 32)
                        ty = int(pos[1] // 32)
                        if (0 <= ty < game_map.rows and 0 <= tx < game_map.cols
                                and game_map.tiles[ty][tx] == 0):
                            self.pickups.append(Pickup(ptype, pos))
                self.spawn_timers[ptype] = defn["respawn"]

        # 更新拾取物动画
        for p in self.pickups:
            p.update(dt)

        # 碰撞检测
        for player in players:
            if not player.alive:
                continue
            for p in self.pickups[:]:
                if not p.alive:
                    continue
                hit, _ = circle_circle_collision(
                    p.pos, p.radius,
                    player.pos, player.radius + 8
                )
                if hit:
                    self._apply(player, p)
                    p.alive = False
                    self.pickups.remove(p)

    def _apply(self, player, pickup):
        if pickup.pickup_type == "health":
            player.heal(HEAL_AMOUNT)
        elif pickup.pickup_type == "power":
            player.power_timer = POWER_DURATION
        elif pickup.pickup_type == "speed":
            player.speed_timer = SPEED_DURATION

    def render(self, screen, camera_offset):
        for p in self.pickups:
            if p.alive:
                p.render(screen, camera_offset)

    def reset(self):
        self.pickups.clear()
        self.spawn_timers = {
            "health": 0.0,
            "power": 0.0,
            "speed": 0.0,
        }
```

- [ ] **Step 2: 验证导入**

```bash
cd "D:\ClaudeCode Program Environment\2Players Shooting Games"
python -c "from src.systems.pickup_manager import PickupManager; pm = PickupManager(); print('OK')"
```

- [ ] **Step 3: 提交**

```bash
git add src/systems/pickup_manager.py
git commit -m "feat: 添加 PickupManager 拾取物刷新/碰撞/效果系统"
```

---

### Task 4: Player 增加 power-up 状态

**Files:**
- Modify: `src/entities/player.py`

玩家需要 power-up 计时器和效果乘数。

- [ ] **Step 1: 在 Player.__init__ 追加字段**

在 `self.effects = []` 之后添加：

```python
        # Power-up 状态 (v1.2.0)
        self.power_timer = 0.0
        self.speed_timer = 0.0
```

- [ ] **Step 2: 在 Player.update 中添加计时器更新**

在 `self.blind_timer = max(0, self.blind_timer - dt)` 之后添加：

```python
        self.power_timer = max(0, self.power_timer - dt)
        self.speed_timer = max(0, self.speed_timer - dt)
```

- [ ] **Step 3: 修改 Player.update 中的速度计算**

将 `speed = PLAYER_SPEED` 后的逻辑改为：

```python
        speed = PLAYER_SPEED
        if self.speed_timer > 0:
            speed *= 1.5
        if self.is_sprinting:
            speed *= SPRINT_SPEED_MULT
```

- [ ] **Step 4: 在 Player 中添加伤害乘数属性**

```python
    @property
    def damage_multiplier(self):
        return 1.5 if self.power_timer > 0 else 1.0
```

- [ ] **Step 5: 在 Player.respawn 中重置 power-up 状态**

在 `self.effects.clear()` 之后添加：

```python
        self.power_timer = 0.0
        self.speed_timer = 0.0
```

- [ ] **Step 6: 验证**

```bash
cd "D:\ClaudeCode Program Environment\2Players Shooting Games"
python -c "from src.entities.player import Player; p = Player(1, (100,100), (255,0,0), {}); print('power_timer:', p.power_timer); print('dmg_mult:', p.damage_multiplier)"
```

- [ ] **Step 7: 提交**

```bash
git add src/entities/player.py
git commit -m "feat(player): 添加 power-up 状态（伤害翻倍/加速计时器）"
```

---

### Task 5: GameMap tile 破坏系统

**Files:**
- Modify: `src/entities/map.py`

实现 destroy_tile、get_tiles_in_radius、rebuild_wall_rects。

- [ ] **Step 1: 在 GameMap.__init__ 中存储动态瓦片状态**

在 `self.tiles = ...` 之后添加：

```python
        # 存储原始数据以便回合重置
        self._original_data = data
        self._original_tiles = [row[:] for row in self.tiles]
        # 移动墙数据
        self.moving_walls = []
        self._load_moving_walls(data.get("moving_walls", []))
```

- [ ] **Step 2: 添加 _load_moving_walls 方法（先创建空壳）**

在 `get_spawn` 方法之前添加：

```python
    def _load_moving_walls(self, mw_data):
        self.moving_walls = []
        for mw in mw_data:
            self.moving_walls.append({
                "tile_x": mw["tile_x"],
                "tile_y": mw["tile_y"],
                "tile_w": mw["tile_w"],
                "tile_h": mw["tile_h"],
                "path": [(p["x"], p["y"]) for p in mw["path"]],
                "current_target": 0,
                "speed": mw.get("speed", 80),
                "pause": mw.get("pause", 0.5),
                "pause_timer": 0.0,
                "moving_forward": True,
            })
            # 在 tiles 中标记为 tile=4
            tx, ty = mw["tile_x"], mw["tile_y"]
            for dy in range(mw["tile_h"]):
                for dx in range(mw["tile_w"]):
                    if 0 <= ty + dy < self.rows and 0 <= tx + dx < self.cols:
                        self.tiles[ty + dy][tx + dx] = 4
```

- [ ] **Step 3: 添加 destroy_tile 方法**

在 `get_spawn` 方法之前添加：

```python
    def destroy_tile(self, tx, ty):
        """摧毁指定瓦片，返回粒子数据列表"""
        if not (0 <= ty < self.rows and 0 <= tx < self.cols):
            return []
        if self.tiles[ty][tx] != 2:
            return []
        self.tiles[ty][tx] = 0
        self.rebuild_wall_rects()
        # 返回碎片粒子数据
        cx = tx * TILE_SIZE + TILE_SIZE // 2
        cy = ty * TILE_SIZE + TILE_SIZE // 2
        import random as rnd
        particles = []
        for _ in range(10):
            angle = rnd.uniform(0, 6.283)
            speed = rnd.uniform(60, 180)
            vx = cx + rnd.uniform(-8, 8)
            vy = cy + rnd.uniform(-8, 8)
            particles.append({
                "pos": (vx, vy),
                "vel": (math.cos(angle) * speed, math.sin(angle) * speed - 100),
                "size": rnd.randint(3, 8),
                "color": (140, 100, 50),
                "life": rnd.uniform(0.4, 1.0),
            })
        return particles

    def get_tiles_in_radius(self, cx, cy, radius):
        """返回圆形区域内 tile=2 的坐标列表"""
        tiles = []
        min_tx = max(0, int((cx - radius) // TILE_SIZE))
        max_tx = min(self.cols - 1, int((cx + radius) // TILE_SIZE))
        min_ty = max(0, int((cy - radius) // TILE_SIZE))
        max_ty = min(self.rows - 1, int((cy + radius) // TILE_SIZE))
        for ty in range(min_ty, max_ty + 1):
            for tx in range(min_tx, max_tx + 1):
                if self.tiles[ty][tx] == 2:
                    tile_cx = tx * TILE_SIZE + TILE_SIZE // 2
                    tile_cy = ty * TILE_SIZE + TILE_SIZE // 2
                    if (tile_cx - cx) ** 2 + (tile_cy - cy) ** 2 <= radius ** 2:
                        tiles.append((tx, ty))
        return tiles

    def rebuild_wall_rects(self):
        """根据当前 tiles 重建碰撞矩形列表"""
        self.wall_rects.clear()
        visited = [[False] * self.cols for _ in range(self.rows)]
        for ty in range(self.rows):
            for tx in range(self.cols):
                if self.tiles[ty][tx] in (1, 2, 3, 4) and not visited[ty][tx]:
                    # 查找水平连续段
                    end_tx = tx
                    while (end_tx + 1 < self.cols
                           and self.tiles[ty][end_tx + 1] in (1, 2, 3, 4)
                           and not visited[ty][end_tx + 1]):
                        end_tx += 1
                    # 垂直扩展
                    end_ty = ty
                    can_extend = True
                    while can_extend and end_ty + 1 < self.rows:
                        for x in range(tx, end_tx + 1):
                            if (self.tiles[end_ty + 1][x] not in (1, 2, 3, 4)
                                    or visited[end_ty + 1][x]):
                                can_extend = False
                                break
                        if can_extend:
                            end_ty += 1
                    # 标记已访问
                    for y in range(ty, end_ty + 1):
                        for x in range(tx, end_tx + 1):
                            visited[y][x] = True
                    w = (end_tx - tx + 1) * TILE_SIZE
                    h = (end_ty - ty + 1) * TILE_SIZE
                    self.wall_rects.append(pygame.Rect(
                        tx * TILE_SIZE, ty * TILE_SIZE, w, h
                    ))
```

- [ ] **Step 4: 导入 math**

在文件顶部 `import json` 之后、`import os` 之前添加：

```python
import math
```

- [ ] **Step 5: 验证**

```bash
cd "D:\ClaudeCode Program Environment\2Players Shooting Games"
python -c "from src.entities.map import load_map; m = load_map('training_yard'); print('tiles:', m.rows, 'x', m.cols); print('wall rects:', len(m.wall_rects))"
```

- [ ] **Step 6: 提交**

```bash
git add src/entities/map.py
git commit -m "feat(map): 添加 tile 破坏系统、移动墙加载、碰撞体重建"
```

---

### Task 6: GameMap 移动墙更新 + 渲染更新

**Files:**
- Modify: `src/entities/map.py`

添加 update 方法驱动物理墙移动，更新渲染支持 tile=3（半高）和 tile=4（移动墙）。

- [ ] **Step 1: 添加 GameMap.update 方法**

在 `rebuild_wall_rects` 方法之后、`get_spawn` 之前添加：

```python
    def update(self, dt):
        """更新移动墙位置"""
        from config import MOVING_WALL_PAUSE
        needs_rebuild = False
        for mw in self.moving_walls:
            if mw["pause_timer"] > 0:
                mw["pause_timer"] -= dt
                continue
            target = mw["path"][mw["current_target"]]
            cur_tx, cur_ty = mw["tile_x"], mw["tile_y"]
            target_tx, target_ty = target
            if cur_tx == target_tx and cur_ty == target_ty:
                mw["pause_timer"] = mw["pause"]
                # 切换到下一目标
                if mw["moving_forward"]:
                    mw["current_target"] += 1
                    if mw["current_target"] >= len(mw["path"]):
                        mw["current_target"] = len(mw["path"]) - 2
                        mw["moving_forward"] = False
                else:
                    mw["current_target"] -= 1
                    if mw["current_target"] < 0:
                        mw["current_target"] = 1
                        mw["moving_forward"] = True
                continue
            # 计算移动
            dx = target_tx - cur_tx
            dy = target_ty - cur_ty
            dist = (dx * dx + dy * dy) ** 0.5
            if dist < 0.01:
                continue
            step = mw["speed"] * dt / TILE_SIZE
            if step >= dist:
                # 到达
                self._move_wall_to(mw, target_tx, target_ty)
                mw["pause_timer"] = mw["pause"]
                if mw["moving_forward"]:
                    mw["current_target"] += 1
                    if mw["current_target"] >= len(mw["path"]):
                        mw["current_target"] = len(mw["path"]) - 2
                        mw["moving_forward"] = False
                else:
                    mw["current_target"] -= 1
                    if mw["current_target"] < 0:
                        mw["current_target"] = 1
                        mw["moving_forward"] = True
            else:
                new_tx = cur_tx + dx / dist * step
                new_ty = cur_ty + dy / dist * step
                self._move_wall_to(mw, new_tx, new_ty)
            needs_rebuild = True
        if needs_rebuild:
            self.rebuild_wall_rects()

    def _move_wall_to(self, mw, new_tx, new_ty):
        """将移动墙平滑移动到新位置（瓦片坐标可为浮点）"""
        old_tx, old_ty = mw["tile_x"], mw["tile_y"]
        # 清除旧位置
        for dy in range(mw["tile_h"]):
            for dx in range(mw["tile_w"]):
                ty_clear = old_ty + dy
                tx_clear = old_tx + dx
                if 0 <= ty_clear < self.rows and 0 <= tx_clear < self.cols:
                    if self.tiles[ty_clear][tx_clear] == 4:
                        self.tiles[ty_clear][tx_clear] = 0
        # 设置新位置（四舍五入到整数瓦片）
        int_tx = round(new_tx)
        int_ty = round(new_ty)
        mw["tile_x"] = int_tx
        mw["tile_y"] = int_ty
        for dy in range(mw["tile_h"]):
            for dx in range(mw["tile_w"]):
                ty_set = int_ty + dy
                tx_set = int_tx + dx
                if 0 <= ty_set < self.rows and 0 <= tx_set < self.cols:
                    self.tiles[ty_set][tx_set] = 4

    def get_moving_wall_rects(self):
        """返回移动墙的像素矩形列表（用于玩家推动检测）"""
        rects = []
        for mw in self.moving_walls:
            rects.append(pygame.Rect(
                mw["tile_x"] * TILE_SIZE,
                mw["tile_y"] * TILE_SIZE,
                mw["tile_w"] * TILE_SIZE,
                mw["tile_h"] * TILE_SIZE,
            ))
        return rects
```

- [ ] **Step 2: 更新 GameMap.render 支持 tile=3（半高）和 tile=4（移动墙）**

将 render 方法中 tile 颜色判断逻辑替换为：

```python
        # Walls
        for ty in range(start_ty, end_ty):
            for tx in range(start_tx, end_tx):
                tile = self.tiles[ty][tx]
                if tile == 0:
                    continue
                rx = tx * TILE_SIZE - ox
                ry = ty * TILE_SIZE - oy
                if tile == 1:
                    color = (80, 80, 90)
                elif tile == 2:
                    color = (140, 100, 50)
                elif tile == 3:
                    color = (100, 100, 120)
                elif tile == 4:
                    color = (60, 70, 90)
                else:
                    color = (100, 100, 120)

                if tile == 3:
                    # 半高掩体：只画下半部分
                    half_h = TILE_SIZE // 2
                    half_ry = ry + TILE_SIZE // 2
                    pygame.draw.rect(screen, color, (rx, half_ry, TILE_SIZE, half_h))
                    pygame.draw.rect(screen, COLOR_OUTLINE, (rx, half_ry, TILE_SIZE, half_h), 1)
                    pygame.draw.line(screen, (150, 150, 160), (rx, half_ry), (rx + TILE_SIZE, half_ry), 2)
                else:
                    pygame.draw.rect(screen, color, (rx, ry, TILE_SIZE, TILE_SIZE))
                    pygame.draw.rect(screen, COLOR_OUTLINE, (rx, ry, TILE_SIZE, TILE_SIZE), 1)
                    pygame.draw.line(screen, (120, 120, 130), (rx, ry), (rx + TILE_SIZE, ry), 1)

                # 移动墙方向箭头
                if tile == 4:
                    cx = rx + TILE_SIZE // 2
                    cy = ry + TILE_SIZE // 2
                    # 查找此墙对应的移动数据
                    for mw in self.moving_walls:
                        if (mw["tile_x"] <= tx < mw["tile_x"] + mw["tile_w"]
                                and mw["tile_y"] <= ty < mw["tile_y"] + mw["tile_h"]):
                            if mw["current_target"] < len(mw["path"]):
                                tgt = mw["path"][mw["current_target"]]
                                dx_arrow = tgt[0] - mw["tile_x"]
                                dy_arrow = tgt[1] - mw["tile_y"]
                                if dx_arrow != 0 or dy_arrow != 0:
                                    arrow_len = 8
                                    angle = math.atan2(dy_arrow, dx_arrow)
                                    ax = cx + int(math.cos(angle) * arrow_len)
                                    ay = cy + int(math.sin(angle) * arrow_len)
                                    pygame.draw.line(screen, (180, 200, 220), (cx, cy), (ax, ay), 2)
                            break
```

- [ ] **Step 3: 验证导入**

```bash
cd "D:\ClaudeCode Program Environment\2Players Shooting Games"
python -c "from src.entities.map import load_map; m = load_map('training_yard'); print('moving walls:', len(m.moving_walls)); print('OK')"
```

- [ ] **Step 4: 提交**

```bash
git add src/entities/map.py
git commit -m "feat(map): 移动墙物理更新 + tile=3 半高掩体 + tile=4 移动墙渲染"
```

---

### Task 7: Environment 环境事件系统

**Files:**
- Create: `src/systems/environment.py`

管理每张地图的专属动态机制：塌方、毒气、重力反转、荆棘陷阱。

- [ ] **Step 1: 创建 Environment 类**

```python
import random
import math
from config import (
    COLLAPSE_INTERVAL_MIN, COLLAPSE_INTERVAL_MAX,
    POISON_START_TIME, POISON_INITIAL_RADIUS,
    POISON_EXPAND_INTERVAL, POISON_EXPAND_STEP, POISON_DPS,
    GRAVITY_FLIP_INTERVAL,
    THORN_ACTIVATE_INTERVAL, THORN_ACTIVATE_COUNT,
    THORN_DURATION, THORN_DAMAGE,
    TILE_SIZE,
)


class Environment:
    def __init__(self):
        self.map_name = ""
        self.active = False

        # Collapse
        self.collapse_timer = 0.0
        self._next_collapse_interval = 0.0

        # Poison gas
        self.poison_active = False
        self.poison_centers = []
        self.poison_radius = 0
        self.poison_expand_timer = 0.0

        # Gravity flip
        self.gravity_flip_timer = 0.0
        self.gravity_inverted = False

        # Thorns
        self.thorn_timer = 0.0
        self.thorn_patches = []
        self.thorn_spots = []

        # Active hazards for rendering
        self.active_hazards = []

    def setup(self, map_name, config_data):
        self.map_name = map_name
        self.active = True
        self._next_collapse_interval = random.uniform(
            COLLAPSE_INTERVAL_MIN, COLLAPSE_INTERVAL_MAX
        )
        self.collapse_timer = self._next_collapse_interval
        self.poison_active = False
        self.poison_centers = []
        self.poison_radius = POISON_INITIAL_RADIUS
        self.poison_expand_timer = POISON_START_TIME
        self.gravity_flip_timer = GRAVITY_FLIP_INTERVAL
        self.gravity_inverted = False
        self.thorn_timer = THORN_ACTIVATE_INTERVAL
        self.thorn_patches = []
        self.thorn_spots = config_data.get("thorn_spots", [])
        self.active_hazards = []

    def update(self, dt, game_map, players):
        if not self.active:
            return
        self.active_hazards = []
        self._update_collapse(dt, game_map)
        self._update_poison(dt, game_map, players)
        self._update_gravity(dt, game_map)
        self._update_thorns(dt, players)

    def _update_collapse(self, dt, game_map):
        if self.map_name != "city_ruins":
            return
        self.collapse_timer -= dt
        if self.collapse_timer <= 0:
            # 随机选取地图边缘的一块 tile=2
            edge_tiles = []
            for ty in range(game_map.rows):
                for tx in range(game_map.cols):
                    if game_map.tiles[ty][tx] == 2:
                        # 靠近边缘（距边界 < 8 格）
                        if (tx < 8 or tx > game_map.cols - 9
                                or ty < 8 or ty > game_map.rows - 9):
                            edge_tiles.append((tx, ty))
            if edge_tiles:
                tx, ty = random.choice(edge_tiles)
                particles = game_map.destroy_tile(tx, ty)
                # 塌方粒子加入 active_hazards 供渲染
                for p in particles:
                    self.active_hazards.append({
                        "type": "debris",
                        "pos": p["pos"],
                        "vel": p["vel"],
                        "size": p["size"],
                        "color": p["color"],
                        "life": p["life"],
                    })
            self._next_collapse_interval = random.uniform(
                COLLAPSE_INTERVAL_MIN, COLLAPSE_INTERVAL_MAX
            )
            self.collapse_timer = self._next_collapse_interval

    def _update_poison(self, dt, game_map, players):
        if self.map_name != "underground_fortress":
            return
        self.poison_expand_timer -= dt
        if self.poison_expand_timer <= 0 and not self.poison_active:
            self.poison_active = True
            self.poison_radius = POISON_INITIAL_RADIUS
            self.poison_expand_timer = POISON_EXPAND_INTERVAL
            # 四角毒气中心
            margin = 5
            self.poison_centers = [
                (margin, margin),
                (game_map.cols - 1 - margin, margin),
                (margin, game_map.rows - 1 - margin),
                (game_map.cols - 1 - margin, game_map.rows - 1 - margin),
            ]
        elif self.poison_active:
            self.poison_expand_timer -= dt
            if self.poison_expand_timer <= 0:
                self.poison_radius += POISON_EXPAND_STEP
                self.poison_expand_timer = POISON_EXPAND_INTERVAL

        if self.poison_active:
            # 毒气伤害
            for player in players:
                if not player.alive:
                    continue
                for cx, cy in self.poison_centers:
                    pcx = cx * TILE_SIZE + TILE_SIZE // 2
                    pcy = cy * TILE_SIZE + TILE_SIZE // 2
                    dist = ((player.pos.x - pcx) ** 2 + (player.pos.y - pcy) ** 2) ** 0.5
                    if dist < self.poison_radius * TILE_SIZE:
                        player.hp = max(0, player.hp - POISON_DPS * dt)
                        if player.hp <= 0:
                            player.alive = False
                        break
            # 毒雾区域加入 hazards
            for cx, cy in self.poison_centers:
                self.active_hazards.append({
                    "type": "poison",
                    "cx": cx * TILE_SIZE + TILE_SIZE // 2,
                    "cy": cy * TILE_SIZE + TILE_SIZE // 2,
                    "radius": self.poison_radius * TILE_SIZE,
                })

    def _update_gravity(self, dt, game_map):
        if self.map_name != "space_station":
            return
        self.gravity_flip_timer -= dt
        if self.gravity_flip_timer <= 0:
            self.gravity_inverted = not self.gravity_inverted
            game_map.gravity_mod = -abs(game_map.gravity_mod) if self.gravity_inverted else abs(game_map.gravity_mod)
            self.gravity_flip_timer = GRAVITY_FLIP_INTERVAL

    def _update_thorns(self, dt, players):
        if self.map_name != "forest_maze":
            return
        self.thorn_timer -= dt

        # 更新激活中的荆棘
        for thorn in self.thorn_patches[:]:
            thorn["timer"] -= dt
            if thorn["timer"] <= 0:
                self.thorn_patches.remove(thorn)
                continue
            # 伤害检测
            for player in players:
                if not player.alive:
                    continue
                tx = int(player.pos.x // TILE_SIZE)
                ty = int(player.pos.y // TILE_SIZE)
                if (tx, ty) == (thorn["tx"], thorn["ty"]):
                    player.hp = max(0, player.hp - THORN_DAMAGE * dt)
                    if player.hp <= 0:
                        player.alive = False

        # 刷新荆棘
        if self.thorn_timer <= 0:
            if self.thorn_spots:
                chosen = random.sample(
                    self.thorn_spots,
                    min(THORN_ACTIVATE_COUNT, len(self.thorn_spots))
                )
                for spot in chosen:
                    self.thorn_patches.append({
                        "tx": spot["x"],
                        "ty": spot["y"],
                        "timer": THORN_DURATION,
                    })
            self.thorn_timer = THORN_ACTIVATE_INTERVAL

        # 荆棘加入 hazards
        for thorn in self.thorn_patches:
            self.active_hazards.append({
                "type": "thorn",
                "tx": thorn["tx"],
                "ty": thorn["ty"],
            })

    def get_active_hazards(self):
        return self.active_hazards

    def reset(self):
        self.setup(self.map_name, {
            "thorn_spots": self.thorn_spots,
        })
```

- [ ] **Step 2: 验证导入**

```bash
cd "D:\ClaudeCode Program Environment\2Players Shooting Games"
python -c "from src.systems.environment import Environment; e = Environment(); e.setup('training_yard', {}); print('OK')"
```

- [ ] **Step 3: 提交**

```bash
git add src/systems/environment.py
git commit -m "feat: 添加 Environment 环境事件系统（塌方/毒气/重力反转/荆棘）"
```

---

### Task 8: BattleScene 集成

**Files:**
- Modify: `src/scenes/battle.py`

将 PickupManager、Environment、GameMap.update 串联进战斗循环，处理破坏触发和渲染。

- [ ] **Step 1: 添加导入**

在现有 import 块末尾添加：

```python
from src.entities.pickup import Pickup
from src.systems.pickup_manager import PickupManager
from src.systems.environment import Environment
```

- [ ] **Step 2: 在 BattleScene.__init__ 中初始化新系统**

在 `self.smoke_clouds = []` 之后添加：

```python
        # v1.2.0 动态环境
        self.pickup_manager = PickupManager()
        self.pickup_manager.setup(
            map_name,
            self.game_map._original_data.get("pickup_spawns", [])
        )
        self.environment = Environment()
        self.environment.setup(
            map_name,
            self.game_map._original_data if hasattr(self.game_map, '_original_data') else {}
        )
        # 存储地图原始数据引用
        self._map_name = map_name
        self._debris_particles = []
```

- [ ] **Step 3: 跳过（_original_data 已在 Task 5 中存储）**

- [ ] **Step 4: 在 BattleScene.update 中添加新系统调用**

在 `self.shake.update(dt)` 之后添加：

```python
        # v1.2.0 动态环境更新
        if self.round_active:
            self.game_map.update(dt)
            self.pickup_manager.update(
                dt,
                [self.player1, self.player2],
                self.game_map
            )
            self.environment.update(
                dt,
                self.game_map,
                [self.player1, self.player2]
            )
            # 更新碎片粒子
            for dp in self._debris_particles[:]:
                dp["life"] -= dt
                dp["pos"] = (
                    dp["pos"][0] + dp["vel"][0] * dt,
                    dp["pos"][1] + dp["vel"][1] * dt + 200 * dt,
                )
                if dp["life"] <= 0:
                    self._debris_particles.remove(dp)
```

- [ ] **Step 5: 在爆炸处理中添加可破坏墙摧毁逻辑**

在 `_explosion` 方法末尾（`self.shake.shake(...)` 之后）添加：

```python
            # 摧毁爆炸范围内的可破坏墙
            tiles_to_destroy = self.game_map.get_tiles_in_radius(
                bullet.pos.x, bullet.pos.y, bullet.explosion_radius
            )
            for tx, ty in tiles_to_destroy:
                debris = self.game_map.destroy_tile(tx, ty)
                self._debris_particles.extend(debris)
```

在 `_trigger_item_effect` 方法中手雷爆炸部分（`self.shake.shake(...)` 之后）添加：

```python
            tiles_to_destroy = self.game_map.get_tiles_in_radius(
                proj.pos.x, proj.pos.y, ITEM_DEFS["grenade"]["explosion_radius"]
            )
            for tx, ty in tiles_to_destroy:
                debris = self.game_map.destroy_tile(tx, ty)
                self._debris_particles.extend(debris)
```

- [ ] **Step 6: 在 render 方法中添加新系统渲染**

在烟雾渲染之后（`screen.blit(smoke_surf, ...)` 之后）、项目渲染之前添加：

```python
            # v1.2.0 拾取物渲染
            self.pickup_manager.render(screen, offset_draw)

            # 碎片粒子渲染
            for dp in self._debris_particles:
                dsx = int(dp["pos"][0] - offset_draw.x)
                dsy = int(dp["pos"][1] - offset_draw.y)
                alpha = int(255 * (dp["life"] / 1.0))
                debris_color = (*dp["color"][:3], min(alpha, 255))
                debris_surf = pygame.Surface((dp["size"] * 2, dp["size"] * 2), pygame.SRCALPHA)
                pygame.draw.rect(
                    debris_surf, debris_color,
                    (0, 0, dp["size"] * 2, dp["size"] * 2)
                )
                screen.blit(debris_surf, (dsx - dp["size"], dsy - dp["size"]))

            # 环境灾害渲染
            for hazard in self.environment.get_active_hazards():
                if hazard["type"] == "poison":
                    hx = int(hazard["cx"] - offset_draw.x)
                    hy = int(hazard["cy"] - offset_draw.y)
                    hr = int(hazard["radius"])
                    fog = pygame.Surface((hr * 2, hr * 2), pygame.SRCALPHA)
                    for ring_r in range(hr, 0, -20):
                        ring_alpha = int(80 * (ring_r / hr))
                        pygame.draw.circle(
                            fog, (50, 200, 50, ring_alpha),
                            (hr, hr), ring_r
                        )
                    screen.blit(fog, (hx - hr, hy - hr))
                elif hazard["type"] == "thorn":
                    # 仅在 tile=0 上绘制遮罩
                    # Skip - 使用地图坐标自行查询
                    hx = hazard["tx"] * 32 - int(offset_draw.x)
                    hy = hazard["ty"] * 32 - int(offset_draw.y)
                    # 红色闪烁方块
                    blink = int(abs(math.sin(pygame.time.get_ticks() * 0.01)) * 150)
                    thorn_surf = pygame.Surface((32, 32), pygame.SRCALPHA)
                    thorn_surf.fill((220, 40, 40, blink))
                    screen.blit(thorn_surf, (hx, hy))
```

- [ ] **Step 7: 在 start_new_round 中重置新系统**

```python
        self.pickup_manager.reset()
        self.environment.reset()
        self._debris_particles.clear()
        # 恢复可破坏墙
        self.game_map.tiles = [row[:] for row in self.game_map._original_tiles]
        self.game_map.rebuild_wall_rects()
        # 重置移动墙
        self.game_map._load_moving_walls(
            self.game_map._original_data.get("moving_walls", [])
        )
```

- [ ] **Step 8: 修改伤害计算使用 power-up 倍率**

在 `_check_bullet_hits` 中 `dmg = target.take_damage(bullet.damage, is_headshot)` 之前，修改 bullet.damage 计算：

找到 `bullet = Bullet(...)` 调用，在其之前（`_handle_shooting` 方法内）添加 damage 修正。更简洁的做法是在 `_check_bullet_hits` 中修改：

```python
        if hit:
            is_headshot = random.random() < 0.12
            # 应用攻击方 power-up 伤害倍率
            attacker = self.player1 if bullet.owner_id == 1 else self.player2
            damage = bullet.damage
            if hasattr(attacker, 'damage_multiplier'):
                damage = int(damage * attacker.damage_multiplier)
            dmg = target.take_damage(damage, is_headshot)
```

- [ ] **Step 9: 验证导入无语法错误**

```bash
cd "D:\ClaudeCode Program Environment\2Players Shooting Games"
python -c "from src.scenes.battle import BattleScene; print('OK')"
```

- [ ] **Step 10: 提交**

```bash
git add src/scenes/battle.py src/entities/map.py
git commit -m "feat(battle): 集成 PickupManager/Environment/GameMap 动态更新 + 破坏渲染"
```

---

### Task 9: 地图 JSON 配置更新

**Files:**
- Modify: `data/maps/training_yard.json`
- Modify: `data/maps/city_ruins.json`
- Modify: `data/maps/underground_fortress.json`
- Modify: `data/maps/space_station.json`
- Modify: `data/maps/forest_maze.json`

为每张地图添加拾取物刷新点、可破坏墙、半高掩体、移动墙和专属机制配置。

- [ ] **Step 1: training_yard.json — 添加移动掩体 + 拾取点**

在现有 walls 数组末尾添加 tile=2/3/4 墙壁区块，并追加新字段。对现有 walls 的 `[10, 15, 13, 2]` 和 `[57, 15, 13, 2]` 改为 tile=3（半高掩体）：

将 walls 数组中第14个元素 `[10, 15, 13, 2]` 改为 `[10, 15, 13, 2, 3]`，第15个 `[57, 15, 13, 2]` 改为 `[57, 15, 13, 2, 3]`。

在 walls 数组末尾追加：

```json
    [38, 22, 4, 3, 2],
    [20, 32, 5, 3, 2],
    [55, 32, 5, 3, 2]
```

在 closing `]` 之后、`}` 之前添加：

```json
  ,
  "pickup_spawns": [
    {"x": 5, "y": 5}, {"x": 74, "y": 5},
    {"x": 40, "y": 38}, {"x": 5, "y": 38}, {"x": 74, "y": 38},
    {"x": 40, "y": 8}, {"x": 20, "y": 25}, {"x": 60, "y": 25},
    {"x": 12, "y": 18}, {"x": 68, "y": 18}
  ],
  "moving_walls": [
    {
      "tile_x": 30, "tile_y": 20, "tile_w": 2, "tile_h": 3,
      "path": [{"x": 30, "y": 20}, {"x": 48, "y": 20}],
      "speed": 70, "pause": 0.5
    }
  ],
  "environment": {"type": "moving_cover"}
```

- [ ] **Step 2: city_ruins.json — 加入可破坏墙 + 拾取点 + 塌方机制**

在 walls 数组追加 tile=2 可破坏墙：

```json
    [25, 30, 3, 2, 2],
    [72, 30, 3, 2, 2],
    [45, 12, 2, 2, 2]
```

追加配置：

```json
  "pickup_spawns": [
    {"x": 8, "y": 3}, {"x": 91, "y": 3},
    {"x": 50, "y": 50}, {"x": 8, "y": 50}, {"x": 91, "y": 50},
    {"x": 50, "y": 30}, {"x": 25, "y": 35}, {"x": 75, "y": 35},
    {"x": 15, "y": 20}, {"x": 85, "y": 20}
  ],
  "environment": {"type": "collapse"}
```

- [ ] **Step 3: underground_fortress.json — 毒气 + 拾取点**

追加 walls tile=2：

```json
    [42, 5, 6, 2, 2],
    [15, 42, 6, 2, 2],
    [68, 42, 6, 2, 2]
```

追加配置：

```json
  "pickup_spawns": [
    {"x": 5, "y": 5}, {"x": 84, "y": 5},
    {"x": 45, "y": 60}, {"x": 5, "y": 60}, {"x": 84, "y": 60},
    {"x": 45, "y": 30}, {"x": 20, "y": 25}, {"x": 70, "y": 40},
    {"x": 10, "y": 45}, {"x": 80, "y": 20}
  ],
  "environment": {"type": "poison_gas"}
```

- [ ] **Step 4: space_station.json — 重力反转 + 拾取点 + 半高掩体**

将中央走廊 walls `[30, 12, 10, 2]` 两侧的某些墙改为 tile=3。在 walls 追加：

```json
    [22, 28, 5, 2, 2],
    [43, 28, 5, 2, 2]
```

追加配置：

```json
  "pickup_spawns": [
    {"x": 5, "y": 5}, {"x": 64, "y": 5},
    {"x": 35, "y": 45}, {"x": 5, "y": 45}, {"x": 64, "y": 45},
    {"x": 35, "y": 20}, {"x": 20, "y": 32}, {"x": 50, "y": 32},
    {"x": 10, "y": 15}, {"x": 60, "y": 15}
  ],
  "environment": {"type": "gravity_flip"}
```

- [ ] **Step 5: forest_maze.json — 荆棘陷阱 + 拾取点**

追加 walls tile=2：

```json
    [48, 25, 4, 2, 2],
    [20, 38, 3, 2, 2],
    [77, 38, 3, 2, 2]
```

追加配置：

```json
  "pickup_spawns": [
    {"x": 3, "y": 3}, {"x": 96, "y": 3},
    {"x": 50, "y": 51}, {"x": 3, "y": 51}, {"x": 96, "y": 51},
    {"x": 50, "y": 27}, {"x": 30, "y": 15}, {"x": 70, "y": 15},
    {"x": 20, "y": 40}, {"x": 80, "y": 40}
  ],
  "thorn_spots": [
    {"x": 10, "y": 10}, {"x": 90, "y": 10}, {"x": 50, "y": 27},
    {"x": 10, "y": 44}, {"x": 90, "y": 44}, {"x": 30, "y": 30},
    {"x": 70, "y": 30}, {"x": 20, "y": 20}, {"x": 80, "y": 20},
    {"x": 40, "y": 40}, {"x": 60, "y": 40}, {"x": 50, "y": 5}
  ],
  "environment": {"type": "thorns"}
```

- [ ] **Step 6: 验证所有 JSON 格式正确**

```bash
cd "D:\ClaudeCode Program Environment\2Players Shooting Games"
python -c "
import json
for name in ['training_yard','city_ruins','underground_fortress','space_station','forest_maze']:
    with open(f'data/maps/{name}.json') as f:
        d = json.load(f)
    print(f'{name}: OK - {len(d.get(\"pickup_spawns\",[]))} spawns')
"
```

- [ ] **Step 7: 提交**

```bash
git add data/maps/training_yard.json data/maps/city_ruins.json data/maps/underground_fortress.json data/maps/space_station.json data/maps/forest_maze.json
git commit -m "feat(maps): 5张地图加入可破坏墙/拾取点/移动墙/专属环境机制"
```

---

### Task 10: 最终验证

- [ ] **Step 1: 完整导入测试**

```bash
cd "D:\ClaudeCode Program Environment\2Players Shooting Games"
python -c "
from config import *
from src.entities.pickup import Pickup, PICKUP_TYPES
from src.systems.pickup_manager import PickupManager
from src.systems.environment import Environment
from src.entities.map import load_map
from src.entities.player import Player
from src.scenes.battle import BattleScene
print('All imports OK')
"
```

- [ ] **Step 2: 快速逻辑测试 — 拾取物碰撞**

```bash
cd "D:\ClaudeCode Program Environment\2Players Shooting Games"
python -c "
from src.systems.pickup_manager import PickupManager
from src.entities.player import Player
from src.entities.map import load_map
from src.entities.pickup import Pickup
m = load_map('training_yard')
p = Player(1, (160, 160), (255,0,0), {})
pm = PickupManager()
pm.setup('training_yard', m._original_data.get('pickup_spawns', []))
# 手动放一个紧贴玩家的拾取物
p.pos.x, p.pos.y = 200, 200
pm.pickups.append(Pickup('health', (200, 200)))
pm.update(0.016, [p], m)
print('Pickup count after:', len(pm.pickups))
print('Player HP:', p.hp)
"
```

- [ ] **Step 3: 运行游戏检查是否崩溃**

```bash
cd "D:\ClaudeCode Program Environment\2Players Shooting Games"
timeout 5 python main.py 2>&1 || true
```

确认无 ImportError 或 pygame 初始化错误。

- [ ] **Step 4: 提交最终修复（如有）**

```bash
git status
```

---

### 完成后

回到主分支合并或发起 PR（参考 `superpowers:finishing-a-development-branch`）。
